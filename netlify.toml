[build]
  base = "."
  command = "npm cache clean --force && npm install --legacy-peer-deps && npm run docs:build"
  publish = "docs/.vuepress/dist"
[build.environment]
  NODE_VERSION = "22.13.0"
  NPM_VERSION = "11.4.2"
